<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="piu_tools.Views.UnlockableSongDetails"
             xmlns:vm="clr-namespace:piu_tools.ViewModels"
             xmlns:ffImage="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms">
    <ContentPage.BindingContext>
        <vm:UnlockableSongDetailsViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" Padding="10">
            <ScrollView>
                <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" Margin="0, 10">
                    <StackLayout Margin="5, 0">
                        <ffImage:CachedImage Source="{Binding SongBanner}" Aspect="AspectFit" RetryCount="3" RetryDelay="3"/>
                    </StackLayout>

                    <StackLayout>
                        <Label Text="{Binding SongTitle}" TextColor="Black" FontSize="25" />
                        <Label Text="{Binding SongArtist}" TextColor="Black" FontSize="20" />
                    </StackLayout>

                    <StackLayout>
                        <Label Text="Singles"></Label>
                        <ListView ItemsSource="{Binding SelectedMusic.Charts}" HasUnevenRows="True" >
                           <ListView.ItemTemplate>
                               <DataTemplate>
                                   <ViewCell>
                                       <ViewCell.View>
                                           <StackLayout Orientation="Vertical">
                                               <CheckBox IsChecked="{Binding Done}" />
                                           </StackLayout>
                                       </ViewCell.View>
                                   </ViewCell>
                               </DataTemplate>
                           </ListView.ItemTemplate>
                        </ListView>

                    </StackLayout>
                    <StackLayout>
                        <Label Text="Doubles"></Label>
                    </StackLayout>

                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
